#!/bin/bash
# Omarchy Turbo Downloader (aria2c + yt-dlp)
# Fast, multi-connection, clipboard-aware downloader for files and streaming media.
# Usage: omarchy-download [URL]

set -euo pipefail

DOWNLOAD_DIR="${XDG_DOWNLOAD_DIR:-$HOME/Downloads}"
mkdir -p "$DOWNLOAD_DIR"

# 1. Header Display
clear
gum style \
  --foreground 212 --border-foreground 99 --border double \
  --align center --width 60 --margin "1 0" --padding "1 2" \
  "🚀 Omarchy Turbo Downloader" "aria2c (16x Multi-Stream) + yt-dlp"

# 2. Get URL (inspect clipboard or use argument)
INPUT_URL="${1:-}"
if [[ -z "$INPUT_URL" ]]; then
  CLIP=$(wl-paste 2>/dev/null | tr -d '\r\n' || true)
  if [[ "$CLIP" =~ ^https?:// ]] || [[ "$CLIP" =~ ^ftp:// ]] || [[ "$CLIP" =~ ^magnet:\? ]]; then
    INPUT_URL="$CLIP"
  fi
fi

echo "Enter or paste your download URL below:"
URL=$(gum input --placeholder "https://example.com/file.zip or YouTube/TikTok/Twitter URL..." --value "$INPUT_URL")

if [[ -z "$URL" ]]; then
  gum style --foreground 214 "⚠️ No URL provided. Exiting..."
  exit 0
fi

# 3. Detect URL Type: Media/Streaming site vs Direct File
IS_MEDIA=0
if [[ "$URL" =~ (youtube\.com|youtu\.be|tiktok\.com|twitter\.com|x\.com|instagram\.com|reddit\.com|twitch\.tv|vimeo\.com|soundcloud\.com|bilibili\.com|fb\.watch|facebook\.com) ]]; then
  IS_MEDIA=1
fi

MODE="direct"
if [[ $IS_MEDIA -eq 1 ]]; then
  echo ""
  gum style --foreground 117 "🎬 Media platform detected! Select format:"
  MODE=$(gum choose \
    "🎬 Best Video (Highest Quality + aria2c 16x)" \
    "🎵 Audio Only (MP3 + Cover Art & Metadata)" \
    "📺 1080p Video (Balanced Size + aria2c 16x)" \
    "⚡ Direct File Download (Raw aria2c)")
fi

echo ""
gum style --foreground 86 "📁 Target Directory: $DOWNLOAD_DIR"
echo ""

# 4. Execute Download based on selection
cd "$DOWNLOAD_DIR"
START_TIME=$(date +%s)
DOWNLOADED_FILE=""

case "$MODE" in
  *"Audio Only"*)
    gum style --foreground 120 "🎵 Downloading and extracting audio (MP3 + Tags)..."
    yt-dlp \
      --downloader aria2c \
      --downloader-args "aria2c:-s 16 -x 16 -k 1M -j 4" \
      --extract-audio \
      --audio-format mp3 \
      --audio-quality 0 \
      --embed-thumbnail \
      --embed-metadata \
      --embed-chapters \
      --no-mtime \
      --progress \
      "$URL"
    ;;

  *"1080p Video"*)
    gum style --foreground 120 "📺 Downloading 1080p Video with aria2c acceleration..."
    yt-dlp \
      --downloader aria2c \
      --downloader-args "aria2c:-s 16 -x 16 -k 1M -j 4" \
      -f "bestvideo[height<=1080]+bestaudio/best[height<=1080]" \
      --merge-output-format mp4 \
      --embed-thumbnail \
      --embed-metadata \
      --embed-chapters \
      --no-mtime \
      --progress \
      "$URL"
    ;;

  *"Best Video"*)
    gum style --foreground 120 "🎬 Downloading Best Video with aria2c acceleration..."
    yt-dlp \
      --downloader aria2c \
      --downloader-args "aria2c:-s 16 -x 16 -k 1M -j 4" \
      --merge-output-format mp4 \
      --embed-thumbnail \
      --embed-metadata \
      --embed-chapters \
      --no-mtime \
      --progress \
      "$URL"
    ;;

  *)
    gum style --foreground 120 "⚡ Downloading via aria2c (16 parallel connections)..."
    aria2c \
      --dir="$DOWNLOAD_DIR" \
      --max-connection-per-server=16 \
      --split=16 \
      --min-split-size=1M \
      --max-concurrent-downloads=4 \
      --continue=true \
      --auto-file-renaming=true \
      --summary-interval=1 \
      --console-log-level=notice \
      --download-result=full \
      --seed-time=0 \
      "$URL"
    ;;
esac

DURATION=$(( $(date +%s) - START_TIME ))

# 5. Desktop Notification
notify-send \
  -i folder-download \
  -a "Omarchy Downloader" \
  "Download Complete! 🚀" \
  "Finished in ${DURATION}s. Saved to ~/Downloads." \
  2>/dev/null || true

echo ""
gum style \
  --foreground 120 --border-foreground 120 --border rounded \
  --padding "0 2" --margin "1 0" \
  "✅ Download Complete (${DURATION}s)!" "Saved to: $DOWNLOAD_DIR"

# 6. Post-Download Action
echo "What would you like to do next?"
ACTION=$(gum choose \
  "📁 Open in Files (Nautilus)" \
  "🎬 Open Latest Download in Default App" \
  "📋 Copy Downloads Path" \
  "🚪 Exit")

case "$ACTION" in
  *"Open in Files"*)
    nautilus "$DOWNLOAD_DIR" >/dev/null 2>&1 &
    ;;
  *"Open Latest"*)
    LATEST=$(ls -t "$DOWNLOAD_DIR" | head -n 1)
    if [[ -n "$LATEST" ]]; then
      xdg-open "$DOWNLOAD_DIR/$LATEST" >/dev/null 2>&1 &
    fi
    ;;
  *"Copy Downloads"*)
    echo -n "$DOWNLOAD_DIR" | wl-copy 2>/dev/null || true
    gum style --foreground 86 "Copied '$DOWNLOAD_DIR' to clipboard!"
    sleep 1
    ;;
  *)
    exit 0
    ;;
esac
